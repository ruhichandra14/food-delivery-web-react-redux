import React, { Component } from 'react';
import MenuApp from '../containers/menu-container';

export default class App extends Component {
  render() {
    return (
        <div>
            <MenuApp/>
        </div>
    );
  }
}
